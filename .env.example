# =============================================================================
# КОНФИГУРАЦИЯ AMNEZIA-WG-EASY VPN СЕРВЕРА
# =============================================================================
# Скопируйте этот файл в .env и заполните своими значениями:
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# ОСНОВНЫЕ НАСТРОЙКИ
# -----------------------------------------------------------------------------

# Язык интерфейса (ru - русский, en - английский, tr, no, pl, fr, de, ca, es)
LANG=ru

# Хеш пароля для входа в Web UI
# Генерация: docker run --rm ghcr.io/w0rng/amnezia-wg-easy wgpw 'ВАШ_ПАРОЛЬ'
# Альтернатива: htpasswd -nbB admin 'ВАШ_ПАРОЛЬ' | cut -d ':' -f 2
PASSWORD_HASH=

# -----------------------------------------------------------------------------
# НАСТРОЙКИ WEB ИНТЕРФЕЙСА
# -----------------------------------------------------------------------------

# TCP порт для Web UI (по умолчанию: 51821)
WEB_PORT=51821

# IP адрес для привязки Web UI (0.0.0.0 - доступ со всех интерфейсов)
WEB_IP=0.0.0.0

# Показывать статистику трафика в UI (true/false)
UI_TRAFFIC_STATS=false

# Включить метрики Prometheus (true/false)
ENABLE_PROMETHEUS_METRICS=false

# -----------------------------------------------------------------------------
# НАСТРОЙКИ WIREGUARD VPN
# -----------------------------------------------------------------------------

# Публичный IP адрес или доменное имя вашего VPN сервера
# Примеры: vpn.example.com или 203.0.113.1
# Узнать IP: curl -4 ifconfig.me
WG_HOST=

# Публичный UDP порт VPN сервера (по умолчанию: 51820)
WG_PORT=51820

# Сетевой интерфейс для проброса трафика (обычно eth0)
# Узнать: ip route | grep default | awk '{print $5}'
WG_DEVICE=eth0

# MTU для клиентов (оставьте пустым для автоопределения)
# Рекомендуется: 1420 для большинства сетей, 1280 для мобильных
WG_MTU=

# Интервал keepalive в секундах (0 - отключено, 25 рекомендуется для NAT)
WG_PERSISTENT_KEEPALIVE=25

# -----------------------------------------------------------------------------
# НАСТРОЙКИ СЕТИ КЛИЕНТОВ
# -----------------------------------------------------------------------------

# Диапазон IP адресов для клиентов (формат: 10.8.0.x)
WG_DEFAULT_ADDRESS=10.8.0.x

# Диапазон IPv6 адресов для клиентов (формат: fd42:42:42::x)
# Используется ULA (Unique Local Address) диапазон для приватной сети
# fd42:42:42::/64 - рекомендуемый диапазон для WireGuard
WG_DEFAULT_ADDRESS_IPV6=fd42:42:42::x

# DNS сервер для клиентов (1.1.1.1 - Cloudflare, 8.8.8.8 - Google)
WG_DEFAULT_DNS=1.1.1.1

# Разрешенные IP адреса для маршрутизации через VPN
# 0.0.0.0/0, ::/0 - весь трафик через VPN (full tunnel, IPv4 + IPv6)
# 0.0.0.0/0 - только IPv4 трафик через VPN
# 10.8.0.0/24, fd42:42:42::/64 - только локальная сеть VPN (split tunnel)
WG_ALLOWED_IPS=0.0.0.0/0, ::/0

# =============================================================================
# ИНСТРУКЦИИ ПО НАСТРОЙКЕ
# =============================================================================
#
# 1. ГЕНЕРАЦИЯ ХЕША ПАРОЛЯ:
#    docker run --rm ghcr.io/w0rng/amnezia-wg-easy wgpw 'мой_пароль'
#
# 2. ОПРЕДЕЛЕНИЕ СЕТЕВОГО ИНТЕРФЕЙСА:
#    ip route | grep default | awk '{print $5}'
#
# 3. ОПРЕДЕЛЕНИЕ ПУБЛИЧНОГО IP:
#    curl -4 ifconfig.me
#
# 4. РЕКОМЕНДАЦИИ ПО БЕЗОПАСНОСТИ:
#    - Используйте сложный пароль (минимум 16 символов)
#    - Не публикуйте файл .env в репозиторий
#    - Регулярно обновляйте Docker образ
#
# =============================================================================
